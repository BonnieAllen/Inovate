%h1= @staffer.first

%h2= @staffer.email

%h2= @staffer.agency

%h2= @staffer.phone

= link_to "Home", root_path
= link_to "Edit", edit_staffer_path(@staffer)
= link_to "Delete", staffer_path(@staffer), method: :delete, data: { confirm: "Are you sure?"}